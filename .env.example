# Drunk Bar — 환경 변수 설정
# 이 파일을 .env로 복사해서 사용하세요: cp .env.example .env

# 서버 설정
PORT=8888
HOST=0.0.0.0
WORKERS=4

# 외부 접속 허용 도메인 (쉼표 구분, * = 전체 허용)
CORS_ORIGINS=*

# 이미지 생성 (선택, 없으면 폴라로이드 placeholder 사용)
OPENAI_API_KEY=

# 에이전트용 LLM (선택)
ANTHROPIC_API_KEY=
